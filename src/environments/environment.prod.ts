export const environment = {
  production: true,
  /** YouTube companion (yt-dlp). Set under ⋯ or at build time. */
  apiBaseUrl: '',
  /** Local Instaloader API origin. Set under ⋯ or at build time. */
  instagramResolverUrl: 'http://localhost:3848',
};
