export const environment = {
  production: true,
  /** YouTube companion (yt-dlp). Set under ⋯ or at build time. */
  apiBaseUrl: '',
  /** Instagram resolver origin (HTTPS). Set under ⋯ or at build time. */
  instagramResolverUrl: '',
};
